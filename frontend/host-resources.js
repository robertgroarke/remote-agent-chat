// The React Native project owns the shared implementation so Metro never has
// to resolve outside android-app. The Web bundle follows the same source.
export * from '../android-app/lib/host-resources.js';
